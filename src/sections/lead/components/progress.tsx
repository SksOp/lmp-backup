import { ClassValue } from 'clsx';
import React from 'react'

interface Props {
    className?: ClassValue;
    date: string;
    icon: string;
    title: string;
    doneBy?: string;
    isDone?: boolean;
}

function Progress() {
  return (
    <div>
      
    </div>
  )
}

export default Progress
